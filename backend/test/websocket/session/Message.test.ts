describe("Session Message", () => {
    it("should pass messages to other session members", async () => {
        
    });

    it("should not send messages back to the sender", async () => {
        
    });
});