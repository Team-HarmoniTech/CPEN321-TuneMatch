<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/background_dark" /> <!-- Background color -->
    <stroke android:width="1.5dp" /> <!-- Border width -->
    <stroke android:color="@color/pointGreen" /> <!-- Border color (in this case, red) -->
    <corners android:radius="20dp" /> <!-- Corner radius -->
</shape>